name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: Lint Code (Simulation)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: echo "npm install würde hier laufen"

      - name: Run Linter
        run: echo "npm run lint würde hier laufen"

  test:
    name: Run Tests (Simulation)
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: echo "npm install würde hier laufen"

      - name: Run Tests
        run: echo "npm test würde hier laufen"

  deploy:
    name: Deploy (Simulation)
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Simulate Deployment
        run: echo "Deployment successful (wird hier simuliert)"
